import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { await prisma.stockIn.updateMany({ where: { loadingLocation: 'PGR Cold' }, data: { loadingLocation: 'PGR COLD' } }); await prisma.siloInventory.updateMany({ where: { location: 'PGR Cold' }, data: { location: 'PGR COLD' } }); console.log('Updated PGR COLD'); } main().catch(console.error).finally(() => prisma.$disconnect());
