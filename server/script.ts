import { PrismaClient } from '@prisma/client'; import { companyHamaliShare } from './src/lib/calc.js'; const prisma = new PrismaClient(); async function main() { const p = await prisma.purchase.findMany({ include: { stockIn: { include: { purchaseOrder: true } }, verification: true } }); let totalFreight = 0; let totalHamali = 0; for(const x of p) { if(x.stockIn.loadingLocation === 'PGR') { totalFreight += Number(x.freightCharge); totalHamali += companyHamaliShare(Number(x.hamaliCharge)); } } console.log('Total missing value (freight + hamali):', totalFreight + totalHamali); } main().catch(console.error).finally(()=>prisma.$disconnect());
