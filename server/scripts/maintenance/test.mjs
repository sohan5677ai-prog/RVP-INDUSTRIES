import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const silos = await prisma.siloInventory.findMany(); console.log(silos); } main().catch(console.error).finally(() => prisma.$disconnect());
