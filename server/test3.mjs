import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const s = await prisma.stockIn.findMany({ where: { loadingLocation: { in: ['KNM Multi', 'PGR Cold', 'PGR COLD'] } }, include: { purchase: { include: { verification: true } } } }); console.log(JSON.stringify(s, null, 2)); } main().catch(console.error).finally(() => prisma.$disconnect());
