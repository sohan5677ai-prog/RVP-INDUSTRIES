# @slack/bolt v4 declares a peer dependency on @types/express v5, but this server
# runs on Express 4 (and uses Bolt only in Socket Mode, which never touches its
# Express receiver). Accept the peer mismatch so installs resolve cleanly.
legacy-peer-deps=true
